@model ProyectoCloudPyme.Models.Usuario
@{
    ViewBag.Title = "Pagina principal";
    Layout = "~/Views/Shared/_LayoutCloudPymeSesion.cshtml";
}

<h2> </h2>
<html>
<head>
    <title></title>
    <link rel="stylesheet" href="~/Content/StyleViewPagina.css" />
    <style>
        #pantalla-dividida {
            display: flex;
            flex-direction: column;
            height:100vh;
        }

        #pantalla-dividida div:nth-of-type(1) {
            flex-grow: 1;
            display: flex;
         
            flex-direction: column;
            flex-wrap: wrap;
            justify-content: center;
            align-content: center;
            text-align: center;
        }

        #pantalla-dividida div:nth-of-type(2) {
            flex-grow: 1;
            align-items: center;
            justify-content: center;
            flex-wrap:nowrap;

        }
        #pantalla-dividida div:nth-of-type(3) {
            flex-grow: 1;
            background-color: #C7E6D7;
            display: flex;
            flex-direction: column;
        }
   
        .pregunta {
            font-family: "Arial", sans-serif;
            color: #333333;
            font-weight: bold;
            font-size: 32px;
            text-align: center;
        }


        #texplico {
            margin-top: 20px;
            text-align: center;
            border-radius: 5px;
            font-weight: bold;
            color: #F6F0E9;
            font-size: 18px;
            background-color: #0F5F54;
            text-decoration: none;
            padding: 15px;
            text-shadow: 1px 1px 1px #333333;
            box-shadow: 1px 1px 1px #333333;
        }
        #UsuariosTable {
            max-width: 800px; /* Ajusta el valor según tus necesidades */
            margin: 0 auto; /* Centra horizontalmente el contenedor */
        }
    </style>

</head>
<body>
  
    <div id="pantalla-dividida">
        <div>
            <h1>Bienvenido <span style="font-weight:bold">@Model.Nombre</span></h1>
        </div>
        <div >
            @if (Model.Pymes.Count() == 0)
            {
                <div id="UsuariosTable">
                <table>
                    <tr>
                        <td colspan="2" style="text-align:center; justify-content:center">Todavía no tienes Pymes agregadas</td>
                    </tr>
                 
                </table>
                </div>
                    }
            else
            {
            <div id="UsuariosTable">
                <table >
                    <thead>
                        <tr>
                            <th colspan="2" style="text-align:center; justify-content:center">Tus Pymes</th>

                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var pyme in Model.Pymes)
                        {
                            <tr>
                                <td>@pyme.Nombre_pyme</td>
                                <td>@pyme.Tipo_pyme</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
            }
        </div>
        <div>
            <div class="preguntas">
                <h2>hola</h2>
                <h2>Adios</h2>
            </div>
            <div class="enlaces">
                <a href="#">Aqui te explicamos como crearla</a>
                <a href="#">Aqui puedes añadir tu Pyme</a>

            </div>
        </div>
    </div>
</body>
</html>